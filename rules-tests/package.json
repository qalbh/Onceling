{
  "name": "onceling-rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore Security Rules unit tests (P2-11). Run against the Firestore emulator.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
