{
  "name": "onceling-rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore Security Rules unit tests (P2-11) and Functions tests for the pairing callables. Both run against the emulator suite.",
  "scripts": {
    "test": "node --test users.test.mjs pairing_rules.test.mjs",
    "test:functions": "node --test pairing_functions.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
